import dynamic from "next/dynamic"

// const Grid = dynamic(() => import("./Grid").then((c) => c.Grid), { ssr: false })

export default function BgWrapper() {
  return (
    <>
      {/* <Grid /> */}
    </>
  )
}
