---
import { kebabCase } from "lodash-es";

interface Props {
  class?: string;
  containerClass?: string[] | string;
  heading: string;
  action?: {
    l: string;
    t: string;
  };
}

const { heading, class: cn, containerClass } = Astro.props;
---

<section id={kebabCase(heading)} class:list={containerClass}>
  <!-- section:base -->
  <slot name="heading" />
  <div class={cn}>
    <slot />
  </div>
  <!-- section:base end -->
</section>
