{
  "private": true,
  "name": "kuro-stuff",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,js,cjs,vue,md}\"",
    "postinstall": "cd ./apps/portfolio && nuxt prepare && cd ../blog && nuxt prepare"
  },
  "devDependencies": {
    "@turbo/gen": "^1.9.7",
    "prettier": "^2.5.1",
    "turbo": "^1.9.7"
  },
  "packageManager": "yarn@1.22.19",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
