{
  "private": true,
  "name": "kuro-site-abomination",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,js,cjs,vue,md}\"",
    "postinstall": "cd ./apps/portfolio && nuxt prepare"
  },
  "devDependencies": {
    "@turbo/gen": "^1.10.9",
    "prettier": "^2.5.1",
    "turbo": "^1.10.12"
  },
  "packageManager": "yarn@1.22.19",
  "engines": {
    "node": ">18"
  }
}
